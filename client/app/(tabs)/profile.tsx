import { View, Text } from 'react-native'
import React from 'react'
import Screen from '@/components/Screen'

const profile = () => {
  return (
    <Screen>
      <Text className='text-heading'>profile</Text>
    </Screen>
  )
}

export default profile